<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:TextSpeaker.ViewModels;assembly=TextSpeaker"
             xmlns:mvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             mvvm:ViewModelLocator.AutowireViewModel="True"
             x:Class="TextSpeaker.Views.MainPage"
             Title="MainPage">
  <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
    <Button Text="Go to Text Speech" Command="{Binding NavigationCommand}" CommandParameter="TextSpeechPage"/>
    <Button Text="Go to Counter" Command="{Binding NavigationCommand}" CommandParameter="CounterPage"/>
    <Button Text="Go to Battery Info" Command="{Binding NavigationCommand}" CommandParameter="BatteryPage"/>
  </StackLayout>
</ContentPage>