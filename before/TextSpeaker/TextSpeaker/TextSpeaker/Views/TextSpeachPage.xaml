<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:TextSpeaker.ViewModels;assembly=TextSpeaker"
             x:Class="TextSpeaker.Views.TextSpeachPage"
             Title="Text Speach">
  <ContentPage.BindingContext>
    <viewModels:TextSpeachPageViewModel/>
  </ContentPage.BindingContext>
  <StackLayout HorizontalOptions="Fill" VerticalOptions="Center" Margin="10">
    <Entry Text="{Binding Text}"/>
    <Button Text="Say Speach!" Command="{Binding SpeachCommand}"/>
  </StackLayout>
</ContentPage>